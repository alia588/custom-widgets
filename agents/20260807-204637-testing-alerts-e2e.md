# Testing, error handling & alerts plan

Branch: `feat/testing-alerts-e2e`  
Date: 2026-08-07

## Goals
- Never-stale embed coverage (local harness + canary on embed-site-seven)
- Auth / CORS / RLS / CRUD ([TEST] only)
- Error boundaries + Resend critical alerts via `ALERT_EMAILS`

## Layout
- `src/lib/alerts.ts` — server Resend + dedupe
- `src/app/api/v1/alerts` — rate-limited client intake
- `src/components/ErrorBoundary.tsx` + `error.tsx` / `global-error.tsx`
- `e2e/*` Playwright suites; `src/**/*.test.ts` Vitest
- `/e2e/harness` when `ENABLE_E2E_HARNESS=true`

## Commands
```bash
npm run test:unit
npm run test:e2e
npm run test:e2e:canary
```

## Env
See `.env.example` — especially `RESEND_*`, `ALERT_*`, `E2E_ADMIN_*`.
